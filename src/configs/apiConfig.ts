const apiConfig = {
    baseUrl: "http://localhost:",

    userApiConfigPort: 8080,
    userApiConfigRoot: "/userService",

    inventoryApiConfigPort: 8081,
    inventoryApiConfigRoot: "/inventoryService"
};

export default apiConfig;